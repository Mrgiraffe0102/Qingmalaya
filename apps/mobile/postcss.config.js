// PostCSS config — Tailwind + Autoprefixer.
// Taro merges this with its internal PostCSS pipeline for both H5 and miniapp.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
}
